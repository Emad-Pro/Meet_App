import 'package:flutter/material.dart';

class HistoryScree extends StatelessWidget {
  const HistoryScree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
